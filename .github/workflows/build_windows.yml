name: Build Windows App

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Windows Release
    runs-on: windows-latest
    permissions:
      contents: write
    env:
      HAS_MSIX: 'false'
      MSIX_PATH: ''
      RELEASE_PATH: ''

    steps:
      - uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64

      - name: Install Dependencies
        run: flutter pub get

      - name: Build Windows App
        run: flutter build windows --release

      - name: Create MSIX Installer
        run: echo n | flutter pub run msix:create

      - name: Locate Build Output
        id: build_output
        shell: powershell
        run: |
          # Priority to x64 path
          $msixFile = Get-ChildItem -Path "build\windows\x64\runner\Release\*.msix" -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $msixFile) {
            $msixFile = Get-ChildItem -Path "build\windows\runner\Release\*.msix" -ErrorAction SilentlyContinue | Select-Object -First 1
          }
          
          if ($msixFile) {
            echo "MSIX_PATH=$($msixFile.FullName)" >> $env:GITHUB_ENV
            echo "HAS_MSIX=true" >> $env:GITHUB_ENV
            Write-Host "Found MSIX: $($msixFile.FullName)"
          } else {
            echo "HAS_MSIX=false" >> $env:GITHUB_ENV
            Write-Host "MSIX not found"
          }
          
          if (Test-Path "build\windows\x64\runner\Release") {
            echo "RELEASE_PATH=build\windows\x64\runner\Release" >> $env:GITHUB_ENV
          } elseif (Test-Path "build\windows\runner\Release") {
             echo "RELEASE_PATH=build\windows\runner\Release" >> $env:GITHUB_ENV
          }

      - name: Create Portable Zip (EXE version)
        run: Compress-Archive -Path "$env:RELEASE_PATH\*" -DestinationPath clinical_warehouse_portable.zip

      - name: Upload Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            ${{ env.MSIX_PATH }}
            clinical_warehouse_portable.zip

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: windows-build
          path: |
            build/windows/x64/runner/Release/*.msix
            build/windows/runner/Release/*.msix
            clinical_warehouse_portable.zip


